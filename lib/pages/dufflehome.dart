import 'package:flutter/material.dart';
class Dufflehome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Duffle home'),
      ),
    );
  }
}
